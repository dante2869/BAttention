@model IEnumerable<Sln.Models.Tratamiento>

@{
    ViewBag.Title = "Index";
}

<section class="title">
    <div class="container">
        <div class="row-fluid">
            <div class="span6">
                <h1>Intervencion</h1>
            </div>
            <div class="span6">
                <ul class="breadcrumb pull-right">
                    <li><a href="index.html">Inicio</a> <span class="divider">/</span></li>
                    <li><a href="#">Administración</a> <span class="divider">/</span></li>
                    <li class="active">Intervención</li>
                </ul>
            </div>
        </div>
    </div>
</section>

<section id="portfolio" class="container main">
    <p>&nbsp;</p>
    <div class="container">
        <p>El Trastorno por Déficit de Atención e Hiperactividad (TDAH) es una de las causas más frecuentes de fracaso escolar y de problemas sociales en la edad infantil. El síndrome aparece en la infancia y puede persistir y manifestarse en al edad adulta.</p>
    </div>
    <p>
        @Html.ActionLink("Crear Intervención", "Create", "Tratamiento", null, new { @class = "btn btn-primary" })
    </p>
    <table class="table table-striped table-hover ">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Estado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaCreacion)
            </th>

            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FechaCreacion)
                </td>

                <td>
                    @Html.ActionLink("Editar", "Edit", new { id = item.Id })
                </td>
            </tr>
        }

    </table>
    <p>&nbsp;</p>
    <p>&nbsp;</p>
    <p>&nbsp;</p>
    <p>&nbsp;</p>
</section>
