@model Sln.Models.Tratamiento

@{
    ViewBag.Title = "Create";
}

<section class="title">
    <div class="container">
        <div class="row-fluid">
            <div class="span6">
                <h1>Crear Tratamiento</h1>
            </div>
            <div class="span6">
                <ul class="breadcrumb pull-right">
                    <li><a href="index.html">Inicio</a> <span class="divider">/</span></li>
                    <li><a href="#">Administración</a> <span class="divider">/</span></li>
                    <li><a href="#">Tratamiento</a> <span class="divider">/</span></li>
                    <li class="active">Crear</li>
                </ul>
            </div>
        </div>
    </div>
</section>

<section id="portfolio" class="container main">


    <div>
        @Html.ActionLink("Back to List", "Index")
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()
        <div class="row-fluid">
            <div class="span4">
                <input type="text" name="Nombre" id="Nombre" required="required" class="input-block-level" placeholder="Nombre" />
            </div>
            
        </div>
        <textarea rows="10" name="Descripcion" id="Descripcion" required="required" class="input-block-level" placeholder="Descripcion"></textarea>
        <input type="submit" value="Submit Comment" class="btn btn-large btn-primary" />
     }
</section>

@*@section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }*@
